<!DOCTYPE html>
<html lang="en">
    <%- include('partials/head',{title: 'Register - Macramé Rose'}) %>
<body>
    <%- include('partials/headerUsers') %>
    <main id="main-register">
        <div class="title-register">
            <h2>CREAR CUENTA</h2>
        </div>
        <form action="/user/register" method="POST" class="create-form" enctype="multipart/form-data">
            <div class="div-network">
                <div class="div-facebookregister"><a href="http://www.facebook.com/">REGISTRARSE CON FACEBOOK</a>
                </div>
                <div class="div-googleregister"><a href="http://www.google.com/">REGISTRARSE CON GOOGLE</a>
                </div>
                <div class="break">
                    <p>o</p>
                </div>
            </div>
            <div class="div-formregister">
                <div class="control-form">
                    <label for="usuario">NOMBRE COMPLETO</label>
                    <input id="usuario" type="text" name="name" class="control" placeholder="Nombre completo" value="<%= locals.oldData ? oldData.name : null %>">
                    <% if (locals.errors && errors.name) {%>
                        <div class="text-danger"><%= errors.name.msg %></div>
                    <% } %>
                </div>
                <div class="control-form">
                    <label for="">NOMBRE DE USUARIO</label>
                    <input type="text" name="userName" class="control" placeholder="Nombre de usuario" value="<%= locals.oldData ? oldData.userName : null %>">
                    <% if (locals.errors && errors.userName) {%>
                        <div class="text-danger"><%= errors.userName.msg %></div>
                    <% } %>
                </div>
                <div class="control-form">
                    <label for="email">EMAIL</label>
                    <input id="email" type="text" name="email" class="control" placeholder="Ej: abcd@efgh.com" value="<%= locals.oldData ? oldData.email : null %>">
                    <% if (locals.errors && errors.email) {%>
                        <div class="text-danger"><%= errors.email.msg %></div>
                    <% } %>
                </div>
                <div class="control-form">
                    <label>Añadir Avatar:</label>
                    <input type="file" name="avatar" class="control">
                    <% if (locals.errors && errors.avatar) {%>
                        <div class="text-danger"><%= errors.avatar.msg %></div>
                    <% } %>
                </div>
                <div class="control-form">
                    <label>CONTRASEÑA</label>
                    <input type="password" name="password" class="control" placeholder="Ingrese contraseña">
                    <% if (locals.errors && errors.password) {%>
                    <div class="text-danger"><%= errors.password.msg %></div>
                    <% } %>
                </div>
                <!-- <div class="control-form">
                    <label>CONFIRMAR CONTRASEÑA</label>
                    <input type="password" name="password" class="control" placeholder="Ingrese contraseña">
                </div> HACER VALIDACION-->
                <div class="div-button-register">
                    <button type="submit" class="button-form">CREAR CUENTA</button>
                    <button type="reset" class="button-form">BORRAR</button>
                </div>
                <div class="div-register-p">
                    <p>¿Ya tienes una cuenta? <a href="/user/login">Iniciar sesión</a></p>
                </div>
            </div>
            
        </form>
    </main>
    <%- include('partials/footerUsers') %>
</body>
</html>